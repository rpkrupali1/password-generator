{
    "configurations": [
        {
            "type": "pwa-chrome",
            "name": "password generator",
            "request": "launch",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}